# AKUMA's Advanced Scanner Configuration - Quick Test
# Quick test for demonstrating IP:port format and PoC functionality

# Target subnets for quick test (small range)
SUBNETS=(
    "192.168.112.237/32"  # Single known vulnerable host
    "192.168.112.54/32"   # Another single host
    "192.168.112.73/32"   # Third host
)

# Parallel execution settings (fast for demo)
MAX_PARALLEL=5
TIMEOUT_PER_HOST=60
NMAP_THREADS=20

# Enable all vulnerability checks for demo
AUTHENTICATED_SCAN=false
DEBUG_MODE=true

# Working directory
WORK_DIR="/home/akuma/Desktop/projects/astral"

# Output format settings
OUTPUT_FORMAT="all"  # TXT, HTML, and exploit guide
INCLUDE_POC=true
INCLUDE_REMEDIATION=true

# Service discovery settings
SCAN_SMB=true
SCAN_RDP=true  
SCAN_HTTP=true
SCAN_SSH=true
SCAN_MSSQL=true
SCAN_WINRM=true
SCAN_FTP=true
